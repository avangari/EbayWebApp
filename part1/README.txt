Project 1 - Naren Nagaragappa & Akshay Vangari

For this project we used the java.io and java.security libraries. We first read the file and stored the contents into a byte array, then computed the SHA1 hash. Finally, we converted the byte array to a string hex value and outputted the result. 